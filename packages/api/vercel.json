{
  "buildCommand": "cd ../.. && pnpm --filter @dashsync/shared build && pnpm --filter @dashsync/wall build && pnpm --filter @dashsync/tablet build && pnpm --filter @dashsync/api build",
  "installCommand": "cd ../.. && pnpm install --frozen-lockfile",
  "framework": null,
  "outputDirectory": "../wall/dist",
  "functions": {
    "api/index.ts": {
      "maxDuration": 30
    }
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
